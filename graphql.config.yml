# Подсказки по полям в gql/GraphQL для расширения GraphQL (VS Code / Cursor).
# Типы для сборки — из pnpm codegen → shared/api/generated/graphql.ts.
schema: src/shared/api/graphql/schema.graphql
documents:
  - src/shared/api/graphql/*/**/*.graphql
  - src/**/*.{ts,tsx}
