/** Сколько товаров показывать в блоке «Подборка» на главной (совпадает с PreloadQuery). */
export const HOME_FEATURED_PRODUCTS_LIMIT = 4;

/** Сколько пользователей подгружать для блока отзывов (фейковые тексты + реальные имена/аватары). */
export const HOME_TESTIMONIAL_USERS_LIMIT = 3;
