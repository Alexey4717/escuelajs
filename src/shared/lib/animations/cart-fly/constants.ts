/** Длительность полёта чипа и задержка перед `addItem` в стор. */
export const CART_FLY_DURATION_MS = 500;

/**
 * Смещение середины траектории по нормали к отрезку «кнопка → корзина» (лёгкая дуга).
 */
export const CART_FLY_ARC_OFFSET_PX = 28;
