/** Upstream Escuela GraphQL (только серверные route handlers, не из браузера). */
export const GRAPHQL_URI = 'https://api.escuelajs.co/graphql';

/** Путь BFF на нашем домене (Apollo ходит сюда; cookie HttpOnly уходят автоматически). */
export const INTERNAL_GRAPHQL_PATH = '/api/graphql';
