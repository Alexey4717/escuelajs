query ProductDetails($id: ID!) {
  product(id: $id) {
    id
    title
    slug
    price
    description
    images
    category {
      id
      name
      slug
      image
    }
    creationAt
    updatedAt
  }
}
