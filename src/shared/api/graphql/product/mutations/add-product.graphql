mutation AddProduct($data: CreateProductDto!) {
  addProduct(data: $data) {
    id
    title
    slug
    price
    description
    images
    category {
      id
      name
      slug
      image
    }
    creationAt
    updatedAt
  }
}
